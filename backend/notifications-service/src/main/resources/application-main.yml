server:
  port: 8283

spring:
  datasource:
    url: jdbc:postgresql://localhost:5455/notificationsdb_main
    username: postgres
    password: yeison
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml