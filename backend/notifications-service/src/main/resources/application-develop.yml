server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/notificationsdb_develop
    username: postgres
    password: yeison
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml